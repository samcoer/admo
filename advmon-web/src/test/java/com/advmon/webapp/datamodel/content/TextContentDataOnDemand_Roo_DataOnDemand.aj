// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.advmon.webapp.datamodel.content;

import com.advmon.webapp.datamodel.AdvertisementDataOnDemand;
import com.advmon.webapp.datamodel.content.TextContent;
import com.advmon.webapp.datamodel.content.TextContentDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect TextContentDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TextContentDataOnDemand: @Component;
    
    private Random TextContentDataOnDemand.rnd = new SecureRandom();
    
    private List<TextContent> TextContentDataOnDemand.data;
    
    @Autowired
    AdvertisementDataOnDemand TextContentDataOnDemand.advertisementDataOnDemand;
    
    public TextContent TextContentDataOnDemand.getNewTransientTextContent(int index) {
        TextContent obj = new TextContent();
        setText(obj, index);
        return obj;
    }
    
    public void TextContentDataOnDemand.setText(TextContent obj, int index) {
        String text = "text_" + index;
        obj.setText(text);
    }
    
    public TextContent TextContentDataOnDemand.getSpecificTextContent(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        TextContent obj = data.get(index);
        Long id = obj.getId();
        return TextContent.findTextContent(id);
    }
    
    public TextContent TextContentDataOnDemand.getRandomTextContent() {
        init();
        TextContent obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return TextContent.findTextContent(id);
    }
    
    public boolean TextContentDataOnDemand.modifyTextContent(TextContent obj) {
        return false;
    }
    
    public void TextContentDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = TextContent.findTextContentEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'TextContent' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<TextContent>();
        for (int i = 0; i < 10; i++) {
            TextContent obj = getNewTransientTextContent(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
