// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.advmon.webapp.datamodel.profiles;

import com.advmon.webapp.datamodel.profiles.TimingProfile;
import com.advmon.webapp.datamodel.profiles.TimingProfileDataOnDemand;
import com.advmon.webapp.datamodel.profiles.TimingProfileIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect TimingProfileIntegrationTest_Roo_IntegrationTest {
    
    declare @type: TimingProfileIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: TimingProfileIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: TimingProfileIntegrationTest: @Transactional;
    
    @Autowired
    TimingProfileDataOnDemand TimingProfileIntegrationTest.dod;
    
    @Test
    public void TimingProfileIntegrationTest.testCountTimingProfiles() {
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", dod.getRandomTimingProfile());
        long count = TimingProfile.countTimingProfiles();
        Assert.assertTrue("Counter for 'TimingProfile' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void TimingProfileIntegrationTest.testFindTimingProfile() {
        TimingProfile obj = dod.getRandomTimingProfile();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to provide an identifier", id);
        obj = TimingProfile.findTimingProfile(id);
        Assert.assertNotNull("Find method for 'TimingProfile' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'TimingProfile' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void TimingProfileIntegrationTest.testFindAllTimingProfiles() {
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", dod.getRandomTimingProfile());
        long count = TimingProfile.countTimingProfiles();
        Assert.assertTrue("Too expensive to perform a find all test for 'TimingProfile', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<TimingProfile> result = TimingProfile.findAllTimingProfiles();
        Assert.assertNotNull("Find all method for 'TimingProfile' illegally returned null", result);
        Assert.assertTrue("Find all method for 'TimingProfile' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void TimingProfileIntegrationTest.testFindTimingProfileEntries() {
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", dod.getRandomTimingProfile());
        long count = TimingProfile.countTimingProfiles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<TimingProfile> result = TimingProfile.findTimingProfileEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'TimingProfile' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'TimingProfile' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void TimingProfileIntegrationTest.testFlush() {
        TimingProfile obj = dod.getRandomTimingProfile();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to provide an identifier", id);
        obj = TimingProfile.findTimingProfile(id);
        Assert.assertNotNull("Find method for 'TimingProfile' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyTimingProfile(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'TimingProfile' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void TimingProfileIntegrationTest.testMergeUpdate() {
        TimingProfile obj = dod.getRandomTimingProfile();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to provide an identifier", id);
        obj = TimingProfile.findTimingProfile(id);
        boolean modified =  dod.modifyTimingProfile(obj);
        Integer currentVersion = obj.getVersion();
        TimingProfile merged = (TimingProfile)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'TimingProfile' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void TimingProfileIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", dod.getRandomTimingProfile());
        TimingProfile obj = dod.getNewTransientTimingProfile(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'TimingProfile' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'TimingProfile' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void TimingProfileIntegrationTest.testRemove() {
        TimingProfile obj = dod.getRandomTimingProfile();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'TimingProfile' failed to provide an identifier", id);
        obj = TimingProfile.findTimingProfile(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'TimingProfile' with identifier '" + id + "'", TimingProfile.findTimingProfile(id));
    }
    
}
