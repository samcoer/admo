// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.advmon.webapp.datamodel;

import com.advmon.webapp.datamodel.EndUser;
import com.advmon.webapp.datamodel.EndUserDataOnDemand;
import com.advmon.webapp.datamodel.EndUserIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect EndUserIntegrationTest_Roo_IntegrationTest {
    
    declare @type: EndUserIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: EndUserIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: EndUserIntegrationTest: @Transactional;
    
    @Autowired
    EndUserDataOnDemand EndUserIntegrationTest.dod;
    
    @Test
    public void EndUserIntegrationTest.testCountEndUsers() {
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", dod.getRandomEndUser());
        long count = EndUser.countEndUsers();
        Assert.assertTrue("Counter for 'EndUser' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void EndUserIntegrationTest.testFindEndUser() {
        EndUser obj = dod.getRandomEndUser();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to provide an identifier", id);
        obj = EndUser.findEndUser(id);
        Assert.assertNotNull("Find method for 'EndUser' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'EndUser' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void EndUserIntegrationTest.testFindAllEndUsers() {
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", dod.getRandomEndUser());
        long count = EndUser.countEndUsers();
        Assert.assertTrue("Too expensive to perform a find all test for 'EndUser', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<EndUser> result = EndUser.findAllEndUsers();
        Assert.assertNotNull("Find all method for 'EndUser' illegally returned null", result);
        Assert.assertTrue("Find all method for 'EndUser' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void EndUserIntegrationTest.testFindEndUserEntries() {
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", dod.getRandomEndUser());
        long count = EndUser.countEndUsers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<EndUser> result = EndUser.findEndUserEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'EndUser' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'EndUser' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void EndUserIntegrationTest.testFlush() {
        EndUser obj = dod.getRandomEndUser();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to provide an identifier", id);
        obj = EndUser.findEndUser(id);
        Assert.assertNotNull("Find method for 'EndUser' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyEndUser(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'EndUser' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void EndUserIntegrationTest.testMergeUpdate() {
        EndUser obj = dod.getRandomEndUser();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to provide an identifier", id);
        obj = EndUser.findEndUser(id);
        boolean modified =  dod.modifyEndUser(obj);
        Integer currentVersion = obj.getVersion();
        EndUser merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'EndUser' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void EndUserIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", dod.getRandomEndUser());
        EndUser obj = dod.getNewTransientEndUser(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'EndUser' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'EndUser' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'EndUser' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void EndUserIntegrationTest.testRemove() {
        EndUser obj = dod.getRandomEndUser();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'EndUser' failed to provide an identifier", id);
        obj = EndUser.findEndUser(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'EndUser' with identifier '" + id + "'", EndUser.findEndUser(id));
    }
    
}
