// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.advmon.webapp.datamodel.profiles;

import com.advmon.webapp.datamodel.profiles.GenderProfile;
import com.advmon.webapp.datamodel.profiles.GenderProfileDataOnDemand;
import com.advmon.webapp.datamodel.profiles.GenderProfileIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GenderProfileIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GenderProfileIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GenderProfileIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GenderProfileIntegrationTest: @Transactional;
    
    @Autowired
    GenderProfileDataOnDemand GenderProfileIntegrationTest.dod;
    
    @Test
    public void GenderProfileIntegrationTest.testCountGenderProfiles() {
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", dod.getRandomGenderProfile());
        long count = GenderProfile.countGenderProfiles();
        Assert.assertTrue("Counter for 'GenderProfile' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GenderProfileIntegrationTest.testFindGenderProfile() {
        GenderProfile obj = dod.getRandomGenderProfile();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to provide an identifier", id);
        obj = GenderProfile.findGenderProfile(id);
        Assert.assertNotNull("Find method for 'GenderProfile' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'GenderProfile' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void GenderProfileIntegrationTest.testFindAllGenderProfiles() {
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", dod.getRandomGenderProfile());
        long count = GenderProfile.countGenderProfiles();
        Assert.assertTrue("Too expensive to perform a find all test for 'GenderProfile', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<GenderProfile> result = GenderProfile.findAllGenderProfiles();
        Assert.assertNotNull("Find all method for 'GenderProfile' illegally returned null", result);
        Assert.assertTrue("Find all method for 'GenderProfile' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GenderProfileIntegrationTest.testFindGenderProfileEntries() {
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", dod.getRandomGenderProfile());
        long count = GenderProfile.countGenderProfiles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<GenderProfile> result = GenderProfile.findGenderProfileEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'GenderProfile' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'GenderProfile' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GenderProfileIntegrationTest.testFlush() {
        GenderProfile obj = dod.getRandomGenderProfile();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to provide an identifier", id);
        obj = GenderProfile.findGenderProfile(id);
        Assert.assertNotNull("Find method for 'GenderProfile' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyGenderProfile(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'GenderProfile' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void GenderProfileIntegrationTest.testMergeUpdate() {
        GenderProfile obj = dod.getRandomGenderProfile();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to provide an identifier", id);
        obj = GenderProfile.findGenderProfile(id);
        boolean modified =  dod.modifyGenderProfile(obj);
        Integer currentVersion = obj.getVersion();
        GenderProfile merged = (GenderProfile)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'GenderProfile' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void GenderProfileIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", dod.getRandomGenderProfile());
        GenderProfile obj = dod.getNewTransientGenderProfile(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'GenderProfile' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'GenderProfile' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void GenderProfileIntegrationTest.testRemove() {
        GenderProfile obj = dod.getRandomGenderProfile();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GenderProfile' failed to provide an identifier", id);
        obj = GenderProfile.findGenderProfile(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'GenderProfile' with identifier '" + id + "'", GenderProfile.findGenderProfile(id));
    }
    
}
